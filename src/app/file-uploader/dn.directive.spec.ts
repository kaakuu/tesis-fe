import { DnDirective } from './dn.directive';

describe('DnDirective', () => {
  it('should create an instance', () => {
    const directive = new DnDirective();
    expect(directive).toBeTruthy();
  });
});
